os: 'linux'
git:
  autocrlf: input
language: node_js
node_js:
  - '14'
notifications:
  email:
    on_success: never
    on_failure: never
jobs:
  include:
    - stage: 'Build and Test'
      name: Format
      script: npm run format:check
    - script: npm run lint
      name: Lint
    - script: npm run build
      name: Build
    - script: npm run test:unit
      name: 'Unit Tests'
